#include<conio.h>
#include<stdlib.h>
#include<math.h>
#include<string.h>
int main(){
int N, key, position=-1;
scanf("%d",&d);
int arr[N];
for(int i=0; i<N; i++){
scanf("%d", &arr[i]);
}
scanf("%d", &key);
for(int i=0; i<n, i++){
if (arr[i]==key){
posoition=i+1;
break;
}
}
if(position !=1){
printf("Element found at position %d\n", position)
}
else{
printf("Element does not found\n")
}
return o ;
}
