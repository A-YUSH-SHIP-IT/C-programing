#include <stdio.h>
#include <string.h>
#include <math.h>
#include <stdlib.h>

#include <stdio.h>
#include <math.h>

int main() {
    int base, exponent;
    double result;
    scanf("%d %d", &base, &exponent);
    result = pow(base, exponent);
    printf("%.0lf\n", result);

    return 0;
}
